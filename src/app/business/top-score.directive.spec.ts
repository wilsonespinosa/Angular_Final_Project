import { TopScoreDirective } from './top-score.directive';

describe('TopScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new TopScoreDirective();
    expect(directive).toBeTruthy();
  });
});
