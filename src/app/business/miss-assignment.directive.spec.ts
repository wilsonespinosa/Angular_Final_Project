import { MissAssignmentDirective } from './miss-assignment.directive';

describe('MissAssignmentDirective', () => {
  it('should create an instance', () => {
    const directive = new MissAssignmentDirective();
    expect(directive).toBeTruthy();
  });
});
